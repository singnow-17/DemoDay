import { extendTheme } from '@chakra-ui/react'

// example theme
const theme = extendTheme({
  fonts: {
    body: "Montserrat",
    heading: "Montserrat",
    mono: "Montserrat",
  },

});

export default theme;